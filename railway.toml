[build]
builder = "dockerfile"
preDeployCommand = "bunx prisma@6 generate"

[deploy]
startCommand = "chmod +x railway-start.sh && bash railway-start.sh"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
