[build]
builder = "dockerfile"

[deploy]
startCommand = "sh railway-start.sh"
preDeployCommand = "bunx prisma generate && bunx prisma db push --skip-generate"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
